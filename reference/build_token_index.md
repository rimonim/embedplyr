# Build hash table to map tokens (i.e. doc_ids) to matrix indices

Build hash table to map tokens (i.e. doc_ids) to matrix indices

## Usage

``` r
build_token_index(embeddings)
```

## Arguments

- embeddings:

  an embeddings object
