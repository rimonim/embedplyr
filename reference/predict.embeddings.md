# Retrieve Token Embeddings

**\[deprecated\]**

`predict.embeddings()` was renamed to
[`emb()`](https://rimonim.github.io/embedplyr/reference/emb.md) to
create a more intuitive API.

## Usage

``` r
# S3 method for class 'embeddings'
predict(object, newdata, drop = TRUE, .keep_missing = FALSE)
```
